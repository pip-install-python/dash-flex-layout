{
 "components": [
  {
   "description": "DashFlexLayout is a wrapper around FlexLayout-React that provides\nflexible docking windows for Dash applications.\n*\nFeatures:\n- Dockable, resizable, and floatable window panels\n- Drag-and-drop tab management\n- Seamless Mantine theme integration (light/dark mode)\n- Dash 2 and Dash 3 compatibility",
   "name": "DashFlexLayout",
   "props": [
    {
     "default": "",
     "description": "Unique ID to identify this component in Dash callbacks.",
     "name": "id",
     "required": false,
     "type": "string"
    },
    {
     "default": "",
     "description": "List of children to be rendered. Children are allocated to their respective tab\nbased on the ID of the element.\n\nWARNING: There is no validation done on whether the children here will be rendered in any tab.\nIf there is no matching tab for a particular ID, that element will be silently ignored in\nrendering (although callbacks will still be applied).",
     "name": "children",
     "required": true,
     "type": "node"
    },
    {
     "default": "",
     "description": "Current color scheme, automatically detected from Mantine theme\nIf not specified, will try to auto-detect from HTML data-mantine-color-scheme",
     "name": "colorScheme",
     "required": false,
     "type": "one of 'light', 'dark'"
    },
    {
     "default": "false",
     "description": "Debug mode flag",
     "name": "debugMode",
     "required": false,
     "type": "bool"
    },
    {
     "default": "",
     "description": "The tab font (overrides value in css).\nExample: font={{size:\"12px\", style:\"italic\"}}",
     "name": "font",
     "required": false,
     "type": "any"
    },
    {
     "default": "",
     "description": "Map of headers to render for each tab. Uses the `onRenderTab` function to override\nthe default headers, where a custom header mapping is supplied.\n\nNote: where possible, it is likely better to use classes to style the headers, rather than\nusing this prop.",
     "name": "headers",
     "required": false,
     "type": "objectOf"
    },
    {
     "default": "",
     "description": "Model layout.",
     "name": "model",
     "required": true,
     "type": "dict"
    },
    {
     "default": "",
     "description": "Imperative FlexLayout action applied to the LIVE model in place \u2014 works in both\nuseStateForModel modes and never re-mounts sibling tabs (unlike replacing `model`,\nwhich is ignored when useStateForModel=true and re-mounts every tab otherwise).\n\nShape: {type, nonce, ...args}. `nonce` must change for the action to fire\n(guard against re-renders re-applying it). Supported types:\n- addNode: {json, toNodeId, location?: 'top'|'bottom'|'left'|'right'|'center', index?, select?}\n  If json.id already exists in the model, the tab is selected instead of re-added.\n- deleteTab: {tabNodeId}\n- selectTab: {tabNodeId}\n- renameTab: {tabNodeId, text}\n- updateNodeAttributes: {nodeId, attributes}\n- adjustWeights: {nodeId, weights}",
     "name": "modelAction",
     "required": false,
     "type": "objectOf"
    },
    {
     "default": "'/assets/popout.html'",
     "description": "URL of popout window relative to origin, defaults to popout.html",
     "name": "popoutURL",
     "required": false,
     "type": "string"
    },
    {
     "default": "",
     "description": "Boolean value, defaults to false, resize tabs as splitters are dragged.\nWarning: this can cause resizing to become choppy when tabs are slow to draw",
     "name": "realtimeResize",
     "required": false,
     "type": "bool"
    },
    {
     "default": "",
     "description": "CSS styles to apply to the root container element",
     "name": "style",
     "required": false,
     "type": "dict"
    },
    {
     "default": "",
     "description": "If left undefined will do simple check based on userAgent",
     "name": "supportsPopout",
     "required": false,
     "type": "bool"
    },
    {
     "default": "false",
     "description": "Flag that we should use internal state to manage the layout. If the layout is not being\nused by dash anywhere (for example, saving and re-hydrating the layout), it is more efficient\nto use the internal state (as this limits the number of round trips between JSON\nand the Model object).\n\nWARNING: If you set this, do not expect the dash property `model` to reflect the current\nstate of the layout!",
     "name": "useStateForModel",
     "required": false,
     "type": "bool"
    }
   ]
  },
  {
   "description": "This is a simple component that holds content to be rendered within a Tab.\nTakes an ID that corresponds to a particular tab in the layout.",
   "name": "Tab",
   "props": [
    {
     "default": "",
     "description": "Unique ID to identify this component in Dash callbacks.",
     "name": "id",
     "required": true,
     "type": "string"
    },
    {
     "default": "",
     "description": "Children to render within Tab",
     "name": "children",
     "required": false,
     "type": "node"
    }
   ]
  }
 ],
 "generated": "2026-08-31"
}
